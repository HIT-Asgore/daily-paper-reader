---
title: Model-Agnostic Meta Learning for Differentiable MPC
title_zh: 可微分模型预测控制的模型无关元学习
authors: "Salma Elfeki, Riccardo Zuliani, Niklas Schmid, Efe C. Balta, John Lygeros"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.19271v1"
tags: ["query:diff-physics"]
score: 8.0
evidence: 可微模型预测控制用于自适应控制
tldr: 模型预测控制(MPC)结合策略优化虽能获得高性能控制器，但易过拟合训练环境，面对新任务时性能大幅下降。为此提出一种模型无关元学习框架，通过元训练使MPC控制器具备快速适应能力，在新任务上仅需少量梯度步骤即可实现高精度控制，计算成本远低于完全重新训练。在球板系统上的参数化轨迹跟踪任务中，该方法展现出出色的自适应能力，为部署鲁棒性提供新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.19271v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 723, \"height\": 421, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19271v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 750, \"height\": 508, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19271v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1805, \"height\": 633, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19271v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 882, \"height\": 570, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19271v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1840, \"height\": 592, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19271v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 901, \"height\": 522, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.19271v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 922, \"height\": 161, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19271v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 921, \"height\": 165, \"label\": \"Table\"}]"
motivation: 现有MPC控制器过拟合训练条件，在未见任务上性能显著下降，缺乏快速适应能力。
method: 提出结合策略优化与模型无关元学习的框架，同时集成系统辨识持续优化超参数和预测模型。
result: 在Ball-on-Plate系统上，该方法能快速适应多种未知轨迹跟踪任务，保持高性能且计算开销低。
conclusion: 元学习赋予MPC快速泛化能力，显著降低重新训练成本，提升部署鲁棒性。
---

## 摘要
将策略优化应用于模型预测控制（MPC）可产生高性能且可靠的控制器。然而，所得控制器往往过拟合其训练条件，并在未见任务中性能显著下降。我们提出了一种将策略优化与元学习相结合的新框架，以训练高度自适应的MPC控制器。我们的方法能够快速适应未见任务，在仅需完整重新训练所需计算成本的一小部分情况下，保持高性能。此外，我们将系统辨识集成到流程中，以持续优化MPC超参数及底层预测模型。我们在球板系统上验证了所提出的方法，展示了在各种参数化轨迹跟踪任务中的卓越适应性。

## Abstract
Applying policy optimization to Model Predictive Control (MPC) yields high-performance and reliable controllers. However, the resulting controllers often overfit their training conditions and suffer significant performance degradation in unseen tasks. We propose a novel framework combining policy optimization with meta-learning to train highly adaptable MPC controllers. Our approach enables rapid adaptation to unseen tasks, maintaining high performance at a fraction of the computational cost required for full retraining. Furthermore, we integrate system identification into the pipeline to continuously refine both the MPC hyperparameters and the underlying predictive models. We validate our proposed methodology on a Ball-on-Plate system, demonstrating superior adaptability across various parameterized trajectory-tracking tasks.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：模型预测控制（MPC）通过策略优化（如梯度调参）可获得高性能控制器，但这类控制器容易过拟合于特定的训练条件（如参考轨迹、系统参数），当遇到未见过的任务时，性能会显著下降。此外，重新训练整个控制器需要多次与真实系统交互，计算成本高、耗时且不适用于硬件部署。
- **研究动机**：为使MPC控制器具备快速适应新任务的能力，同时避免昂贵的全再训练，需要一种能够利用少量交互数据就能泛化的训练范式。
- **整体含义**：本文提出将**模型无关元学习（MAML）** 与**可微分MPC策略优化**框架相结合，使MPC控制器能够从多个训练任务中学习一个可快速适应的初始参数，在未见任务上只需一次或几次闭环交互即可达到良好性能。同时，集成在线系统辨识，持续优化预测模型和控制器参数，进一步提升适应效率。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：利用MAML的“学习如何学习”思想，在多个参考轨迹任务上训练一个**通用参数 \(\bar{p}\)**，使得对于任意新任务，通过一次梯度下降即可得到任务专用参数 \(p_r = \bar{p} - \alpha \nabla_p C_{\text{full}}(r, \bar{p}, \theta_r)\)，从而快速适应。
- **关键技术细节**：
  1. **MPC策略优化框架**：将MPC的代价矩阵 \(P_x, Q_x, R_u\) 参数化为可训练参数 \(p\)，并利用BP-MPC（Backpropagation-MPC）方法计算闭环成本对 \(p\) 的梯度（通过链式法则和隐函数定理），实现端到端调参。
  2. **系统辨识**：采用递归最小二乘（RLS）估计模型参数 \(\theta_r\)（如物理参数 \(\theta_I, \theta_s, \theta_c\)），以提高预测模型精度并减少梯度估计偏差。
  3. **元学习目标**：最小化元目标函数 \(\bar{C}(\bar{p}) = \sum_{r \in \mathcal{R}} C_{\text{full}}(r, p_r(\bar{p}), \theta_r)\)，其中 \(p_r(\bar{p}) = \bar{p} - \alpha \nabla_p C_{\text{full}}(r, \bar{p}, \theta_r)\)。为避免计算二阶Hessian，采用BFGS拟牛顿法近似Hessian矩阵，将元梯度表示为 \(\nabla \bar{C}(\bar{p}) \approx \sum_{r} (I - \alpha H_r)^\top \nabla_p C_{\text{full}}(r, p_r, \theta_r)\)。
- **算法流程（文字说明）**：
  - **外循环**：交替为每个训练任务 \(r\) 执行：① 用当前 \(\bar{p}^k\) 和 \(\theta_r^k\) 通过BP-MPC估计 \(\nabla_p C_{\text{full}}(r, \bar{p}^k, \theta_r^k)\)；② 计算一步更新后的任务参数 \(p_r^k = \bar{p}^k - \alpha \nabla_p\)；③ 在真实系统上运行MPC获得轨迹数据；④ 通过RLS更新 \(\theta_r^{k+1}\)；⑤ 用更新后的 \(\theta_r^{k+1}\) 和 \(p_r^k\) 估计梯度 \(\nabla_p C_{\text{full}}(r, p_r^k, \theta_r^k)\)。然后利用所有任务的梯度及BFGS近似计算元梯度 \(\nabla \bar{C}(\bar{p}^k)\)，更新 \(\bar{p}^{k+1}\)。
  - **内循环（单任务）**：对于新任务，只需一次梯度更新（式(8)）即可获得任务专用参数，无需完整元训练。

### 3. 实验设计：使用了哪些数据集/场景？benchmark及对比方法

- **实验平台**：Ball-on-Plate（BoP）硬件系统（如图1,2），状态为球位置和速度（\(s_x, v_x, s_y, v_y\)），输入为板倾斜角（\(\alpha_x, \alpha_y\)）。采样时间0.025s，预测时域 \(N=20\)。
- **训练参考轨迹**：两个参数化轨迹 —— 半径0.2m的圆（Circle）和边长0.3m的正方形（Square），每个轨迹重复5次，周期5秒。
- **测试参考（未见任务）**：八字轨迹（figure-eight），用于评估元学习后的泛化能力。
- **对比方法**：
  - PID控制器
  - LQR控制器（最优LQR增益）
  - 未调参的MPC（初始参数）
  - 预辨识模型的MPC（使用离线系统辨识得到的\(\theta\)）
  - BP-MPC（不带系统辨识）
  - BP-MPC + 系统辨识（即完全版的单任务优化，Algorithm 4）
  - 元初始化BP-MPC（使用MAML得到的\(\bar{p}\)做一步更新）
- **评估指标**：闭环跟踪成本 \(C_{\text{track}} = \sum_{t=\tau}^T \|x_t - r_t\|_Q^2\)（忽略前200步瞬态），以及总成本 \(C_{\text{full}}\)。

### 4. 资源与算力

- **硬件**：实验在配备Intel i7-1165G7 CPU的台式机上运行，未使用GPU。
- **软件**：使用CasADi和DAQP求解器进行MPC求解和梯度计算。
- **训练时间**：文中未明确给出具体训练时长，仅提及每次闭环交互（迭代）包含一次真实系统运行和一次BP-MPC梯度计算。由于是硬件实验，每轮迭代需等待系统复位（约几秒），但未统计总耗时。
- **算力说明**：本文未报告GPU型号/数量或精确训练时间，主要强调计算开销低（相比于完全重新训练）。

### 5. 实验数量与充分性

- **实验分组**：
  - **实验A**（验证BP-MPC提升效果）：单任务优化（Circle和Square各20轮迭代），对比PID、LQR、未调参MPC、预辨识MPC、BP-MPC。给出了表I（平均成本及标准差）和图3-4（轨迹和收敛曲线）。
  - **实验B**（消融系统辨识）：对比三种配置——①全系统辨识（Algorithm 4）；②无系统辨识；③离线预辨识但不再更新。各任务上均运行20轮，如图6。
  - **实验C**（元学习效果）：使用Circle和Square作为训练任务，学习\(\bar{p}\)，然后测试在Circle、Square及未见八字轨迹上的性能。对比了使用不同任务专用参数（\(p_{\text{circle}}, p_{\text{square}}, p_r(\bar{p})\)）时的成本（表II），以及元初始化BP-MPC与直接BP-MPC的收敛曲线（图5）。
- **充分性分析**：
  - ✅ **覆盖充分**：包含多项消融（系统辨识影响、元学习影响），对比了多种基线（PID, LQR, 未调参MPC, 单任务优化）。
  - ✅ **结果客观**：报告了多次运行的平均值和标准差（表I, II），并展示了具体轨迹图和成本收敛图。
  - ⚠️ **局限性**：仅使用一个硬件平台（BoP），参考轨迹类型有限（仅2个训练+1个测试），未在更多系统上验证泛化性；未做多次随机初始化实验（但报告了标准差，暗示有多次重复）。

### 6. 论文的主要结论与发现

1. **BP-MPC有效提升MPC性能**：未调参MPC的跟踪误差仅为PID的约1/3；经系统辨识后误差再减1/3；再经BP-MPC调参后额外降低约10%。最终BP-MPC成本仅为PID的15%～17%（表I）。
2. **系统辨识显著加速收敛并提高最终性能**：与无系统辨识相比，BP-MPC+系统辨识在20轮迭代后成本降低约两倍；即使使用离线预辨识模型，BP-MPC仍能进一步降低成本（33%～44%）。
3. **元学习提供快速适应能力**：通过MAML得到的通用参数\(\bar{p}\)在未见八字轨迹上产生的成本低于未调参MPC，且远低于使用其他任务专用参数（跨任务泛化差）。进一步，元初始化后再进行少量在线BP-MPC更新，成本可再降低超20%。
4. **计算开销低**：元学习仅在训练阶段需多次系统交互；对于新任务，只需一次闭环互动（用于一步梯度更新）即可获得适应参数，远小于完全重新训练的成本。

### 7. 优点：方法或实验设计上的亮点

- **创新性**：首次将MAML应用于MPC控制器的代价函数和约束参数的元学习（而非仅用于系统模型），解决了MPC策略优化泛化性差的根本问题。
- **端到端集成**：巧妙地将系统辨识、梯度估计、元学习融合进一个联合优化流程，三个环节相互促进（系统辨识改善梯度估计，元学习提供初始化，BP-MPC进一步精调）。
- **硬件验证**：在真实Ball-on-Plate系统上完成实验，而非仿真，验证了方法的实际可行性。
- **计算友好**：使用BFGS近似Hessian避免了二阶导数计算，使元梯度估计高效可行。
- **清晰的消融实验**：分别评估了BP-MPC、系统辨识、元学习的贡献，结论有说服力。

### 8. 不足与局限

- **实验覆盖有限**：仅在一个硬件平台（BoP）上验证，系统动力学较为简单（球-平板），参考轨迹仅3种（圆、方、八字），未在其他复杂系统（如无人机、机械臂）或更丰富任务集上测试。
- **理论假设不严格**：分析依赖于“存在某个\(\theta^*\)使得\(f = f_{\theta^*}\)”的假设，但实际系统可能不满足该条件，收敛性分析未完全覆盖。
- **安全保证缺失**：论文提到“未来工作需加强适应过程的安全保证”，当前方法未考虑在线适应时的约束违背风险（例如适应初期可能暂时违反约束）。
- **未充分讨论梯度估计偏差**：BP-MPC使用近似模型\(f_\theta\)来求梯度，即使系统辨识持续更新，仍可能存在模型误差导致的梯度偏差对元学习的影响未量化分析。
- **资源细节不充分**：未报告训练所需总时间或具体迭代次数（虽然展示了20轮收敛曲线，但未说明达到停止准则的具体轮数）。
- **对异常情况的鲁棒性未知**：未探讨参考轨迹突然变化或系统参数突变（如摩擦力变化）下的适应能力。

（完）
