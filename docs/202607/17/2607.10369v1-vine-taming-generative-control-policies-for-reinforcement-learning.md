---
title: "VINE: Taming Generative Control Policies for Reinforcement Learning"
title_zh: "VINE: 驯服强化学习中的生成式控制策略"
authors: "Rushuai Yang, Zhuo Han, Houlin Li, Hecheng Wang, Zhichao Wu, Rui Zhang, Zhaowei Zhang, Zihong Chen, Xiaohan Yan, Chiming Liu, Yi Chen, Wei Shan, Maoqing Yao"
date: 2026-07-11
pdf: "https://arxiv.org/pdf/2607.10369v1"
tags: ["query:mbrl-manip"]
score: 8.0
evidence: 稳定机器人学习中流匹配策略的价值梯度强化学习
tldr: 流匹配策略在机器人学习中表现出高效建模复杂动作分布的能力，但将其与值梯度强化学习结合时存在训练不稳定性。现有工作通常将不稳定性归因于迭代生成过程，而本文发现根源在于行为克隆中使用的朴素采样策略。为此提出VINE方法，通过在每个去噪步重建插值状态，构建稳定的可微路径用于值梯度传播，同时保持迭代生成和高表达能力。在OGBench离线强化学习基准和真实机器人操作任务上，VINE均取得了领先性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10369v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1414, \"height\": 463, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 778, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1436, \"height\": 538, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1300, \"height\": 644, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1284, \"height\": 742, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1389, \"height\": 1347, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1389, \"height\": 1347, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10369v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1465, \"height\": 427, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10369v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1433, \"height\": 208, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10369v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1500, \"height\": 653, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10369v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1213, \"height\": 389, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10369v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1544, \"height\": 1685, \"label\": \"Table\"}]"
motivation: 流匹配策略在值梯度强化学习中存在训练不稳定问题，现有方法牺牲了迭代生成或表达能力，需要在不牺牲关键特性的前提下实现稳定优化。
method: 提出VINE，一种面向强化学习的采样方法，在每个去噪步重建插值状态，替代原始采样策略，实现端到端值梯度传播的稳定性。
result: 在OGBench离线强化学习基准和真实机器人操作任务上，VINE优于现有最先进方法，且训练过程稳定。
conclusion: VINE证明了通过改进采样策略而非牺牲迭代生成，可以实现流匹配策略在值梯度强化学习中的稳定训练和性能提升。
---

## 摘要
流匹配策略已成为机器人学习中一种有效的策略参数化方法。它从噪声中迭代生成动作，从而能够高度表达复杂且多模态的动作分布。然而，先前的研究观察到，使用值梯度强化学习（RL）来扩展这些策略常常会导致训练不稳定。现有方法将这种不稳定性归因于迭代生成，因此通过牺牲迭代生成、高表达性或值梯度优化来避免端到端的值梯度优化。与先前观点相反，我们表明不稳定性并非源于迭代生成本身，而是源于最初为行为克隆设计的朴素采样策略，该策略在值梯度强化学习下变得脆弱。基于这一见解，我们提出了VINE，一种面向强化学习的采样方法，能够实现对流匹配策略进行稳定的端到端值梯度优化。VINE并非遵循单一的流轨迹，而是在每个去噪步骤中重建一个新的插值状态，为值梯度传播创建一条稳定的可微路径，同时保持与原始流匹配去噪过程的兼容性。因此，VINE保留了流匹配的表达性和迭代生成，而无需牺牲端到端的值梯度优化。尽管通过所有十个去噪步骤进行端到端反向传播，VINE仍能实现稳定的策略改进，并在OGBench离线强化学习基准和真实世界机器人操作任务中持续优于最先进的强化学习方法。视频可在我们的网站上获取：https://agibottech.github.io/vine。

## Abstract
Flow-matching policies have emerged as an effective policy parameterization for robot learning. They iteratively generate actions from noise, enabling highly expressive modeling of complex and multimodal action distributions. However, prior works observed that scaling these policies with value-gradient reinforcement learning (RL) often leads to training instability. Existing methods attribute this instability to iterative generation and therefore avoid end-to-end value-gradient optimization by sacrificing iterative generation, high expressiveness, or value-gradient optimization. Contrary to prior belief, we show the instability does not stem from iterative generation itself, but from the vanilla sampling strategy originally designed for behavior cloning, which becomes brittle under value-gradient RL. Motivated by this insight, we propose VINE, an RL-oriented sampling method that enables stable end-to-end value-gradient optimization for flow-matching policies. Instead of following a single flow trajectory, VINE reconstructs a new interpolation state at every denoising step, creating a stable differentiable path for value-gradient propagation while remaining compatible with the original flow-matching denoising process. As a result, VINE preserves the expressiveness and iterative generation of flow-matching without sacrificing end-to-end value-gradient optimization. Despite performing end-to-end backpropagation through all ten denoising steps, VINE achieves stable policy improvement and consistently outperforms state-of-the-art RL methods on the OGBench offline RL benchmark and real-world robotic manipulation task. Videos are available on our website: https://agibottech.github.io/vine.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：流匹配（flow-matching）等生成式控制策略在机器人学习的行为克隆中表现出色，能够建模复杂、多模态的动作分布。然而，当使用值梯度强化学习（RL）进行端到端微调时，训练过程往往出现严重不稳定性。
- **背景与问题**：现有方法将不稳定性归因于迭代生成过程，因此采取三种妥协策略：①冻结生成策略参数，用外部引导信号调节去噪（牺牲表达性）；②将多步生成蒸馏为单步策略（牺牲迭代生成）；③放弃批评者的动作梯度，仅用标量值加权（牺牲值梯度优化）。这些方法本质上都回避了通过整个去噪链反向传播值梯度，但引入了根本性折衷，导致性能次优。

## 2. 论文提出的方法论

- **核心思想**：作者重新审视不稳定性的根源，发现并非迭代生成本身，而是行为克隆中使用的朴素采样策略（如欧拉ODE采样器）在值梯度优化下变得脆弱。为此提出**VINE（Value-gradient Iterative Noise Exploration）**，一种面向RL的采样方法。
- **关键技术细节**：
  - VINE在每个去噪步骤中，不沿用单一噪声轨迹，而是基于当前动作估计重建一个插值状态（ `ˆx_k = t_k * ˆa_k + (1 - t_k) * z_k`，其中 `z_k` 为独立采样的高斯噪声），然后使用预训练的流匹配速度场计算新的动作估计（ `ˆa_{k+1} = ˆx_k + (1 - t_k) * v_θ(ˆx_k, t_k; s)` ）。
  - 这种重建保证了每个网络查询与流匹配训练语义兼容，同时梯度的反向传播路径稳定。
  - 算法流程（文字说明）：
    1. 初始化动作估计 `ˆa_0 ~ N(0, I)`。
    2. 对于 `k = 0,...,K-1`：
        - 计算时间步 `t_k = k / K`。
        - 生成独立噪声 `z_k ~ N(0, I)`。
        - 重建插值状态 `ˆx_k = t_k * ˆa_k + (1 - t_k) * z_k`。
        - 用速度场预测新动作 `ˆa_{k+1} = ˆx_k + (1 - t_k) * v_θ(ˆx_k, t_k; s)`。
    3. 最终动作 `a = ˆa_K`。
  - 此外，VINE保持了与预训练流匹配策略的兼容性，无需修改生成目标或辅助指导模型。

## 3. 实验设计

- **离线RL设置**：
  - 基准测试：**OGBench**，包含8个领域（antmaze-large, antmaze-giant, humanoidmaze-medium, humanoidmaze-large, scene-sparse, puzzle-3x3-sparse, cube-double, cube-triple），每个领域5个任务，共40个任务。
  - 对比方法：按梯度传播方式分组——高斯策略（ReBRAC等）；Around the chain（FQL, FAWAC, IFQL, QAM, DAC, QSM, CGQL变体, DSRL, FEdit, FBRAC, BAM）；Through the chain（直接BPTT的基线）。覆盖了多种代表性方法。
- **真实世界在线RL设置**：
  - 任务：**插头插入插座**（socket insertion），要求机器人从桌面拾起插头并精确插入，具有紧密接触公差。
  - 对比方法：SAC-Flow、Hil-SERL、DSRL、RLT、EXPO，均使用图像和本体感受状态输入。
- **消融实验**：
  - 采样器消融：VINE vs. 标准欧拉采样器（固定轨迹，仅初始噪声）。
  - 迭代步数消融：`K=1,4,6,8,10` 逐步增加的效果。

## 4. 资源与算力

- 文中未明确说明使用的GPU型号、数量、训练时长等具体算力信息。仅在附录中提及使用Adam优化器、1M梯度步（state-based OGBench任务），以及50个episodes进行评估。实际计算资源未提及。

## 5. 实验数量与充分性

- **实验数量**：
  - 离线RL：40个任务 × 12个种子 → 480次训练，报告均值和95%置信区间（bootstrapping 5000样本）。
  - 在线RL：真实世界20次试验，报告成功率、训练时间、人类干预比例。
  - 消融实验：包括采样器对比（5个任务）和迭代步数分析（AntMaze任务中的梯度范数和成功率变化）。
- **充分性与客观性**：
  - 离线实验覆盖了多种难度的导航和操作任务，对比方法全面，统计评估严谨。
  - 在线实验在真实机器人上开展，具有实际验证价值。
  - 消融实验直接验证了VINE中两个关键设计（随机插值和迭代精细）的作用，证据链条清晰。
  - 不足之处：在线实验规模较小（仅一个任务），可能不足以推广到所有操作场景；此外，文中未对VINE与其他through-the-chain方法（如直接BPTT的基线）进行系统的定量对比，仅在消融中对比了欧拉采样器。

## 6. 论文的主要结论与发现

- **主要结论**：
  - 不稳定性并非源于迭代生成，而是源于采样策略；通过设计RL导向的采样器VINE，可实现稳定的端到端值梯度反向传播，无需牺牲迭代生成、表达性或值梯度优化。
  - VINE在OGBench的40个任务中取得了最高的平均得分（60% vs. 次优55%），尤其在长程导航任务（antmaze-giant、humanoidmaze-large）上优势明显。
  - 在真实机器人插头插入任务中，VINE以20/20成功率、最短微调时间（20分钟）和最低人类干预率（19.2%）全面优于对比方法。
  - 消融实验证实：VINE的梯度范数更小、更稳定，且迭代步数越多动作质量越高（从K=1的0%成功率提升到K=10的90%）。

## 7. 优点

- **方法创新性**：精准定位问题根源（采样策略而非迭代生成），提出简单有效的采样重设计，无需修改模型架构或损失函数。
- **实验完备性**：离线40任务+真实机器人+多种消融，统计严谨（12种子、置信区间）。
- **理论与实践结合**：提供理论定理（FM-VINE一致性、确定性VINE的收缩性）保证采样与预训练兼容。
- **实用价值**：VINE可直接应用于预训练流匹配策略，仅需更换采样器，降低了使用门槛。
- **可推广性**：作者指出VINE可扩展到扩散策略等其他迭代生成策略。

## 8. 不足与局限

- **实验覆盖**：在线实验仅一个真实世界任务，且任务复杂度有限（单步插拔），缺乏多任务、长时域、更具挑战性的验证。
- **对比方法**：虽然对比了众多baseline，但未与同样做through-the-chain的近期工作（如ReinFlow、FlowDPG）直接比较，可能遗漏关键对比。
- **计算资源未披露**：缺少训练耗时、GPU型号等细节，影响可复现性评估。
- **适用性限制**：VINE依赖预训练流匹配策略，若预训练质量差或分布外严重，效果可能下降；此外，对于超高维度动作空间（如全身控制），十个去噪步的BPTT可能带来较大内存开销。
- **理论分析**：虽然给出了兼容性定理，但对稳定性的正式理论证明（如梯度方差上界）尚不充分，更多是经验验证。

（完）
