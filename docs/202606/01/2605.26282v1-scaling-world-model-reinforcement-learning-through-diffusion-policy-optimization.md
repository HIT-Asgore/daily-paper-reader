---
title: Scaling World-Model Reinforcement Learning Through Diffusion Policy Optimization
title_zh: 通过扩散策略优化扩展基于世界模型的强化学习
authors: "Xiaoyuan Cheng, Wenxuan Yuan, Zhancun Mu, Yuanzhao Zhang, Yiming Yang, Hai Wang, Zhuo Sun, Che Liu"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.26282v1"
tags: ["query:mbrl-manip"]
score: 9.0
evidence: 基于世界模型的模型基强化学习用于扩展
tldr: 基于模型的强化学习受模型偏差和搜索-价值学习错配限制。提出MBDPO，在潜在世界模型中将策略优化转化为扩散过程，利用隐式能量函数统一搜索与优化。在多任务离线预训练、在线学习和微调中表现优异，离线预训练随模型容量增大性能持续提升。该框架为可扩展世界模型策略学习提供了新方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26282v1/fig-001.webp\", \"caption\": \"\", \"page\": 3, \"index\": 1, \"width\": 1100, \"height\": 680}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-002.webp\", \"caption\": \"\", \"page\": 3, \"index\": 2, \"width\": 1099, \"height\": 680}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-003.webp\", \"caption\": \"\", \"page\": 3, \"index\": 3, \"width\": 383, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-004.webp\", \"caption\": \"\", \"page\": 3, \"index\": 4, \"width\": 1100, \"height\": 680}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-005.webp\", \"caption\": \"\", \"page\": 3, \"index\": 5, \"width\": 1100, \"height\": 680}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-006.webp\", \"caption\": \"\", \"page\": 6, \"index\": 6, \"width\": 5192, \"height\": 1650}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-007.webp\", \"caption\": \"\", \"page\": 6, \"index\": 7, \"width\": 4050, \"height\": 1399}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-008.webp\", \"caption\": \"\", \"page\": 21, \"index\": 8, \"width\": 1659, \"height\": 948}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-009.webp\", \"caption\": \"\", \"page\": 21, \"index\": 9, \"width\": 1644, \"height\": 957}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-010.webp\", \"caption\": \"\", \"page\": 22, \"index\": 10, \"width\": 1600, \"height\": 540}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-011.webp\", \"caption\": \"\", \"page\": 22, \"index\": 11, \"width\": 1991, \"height\": 789}]"
motivation: 现有世界模型方法中搜索与价值学习结构错配，导致训练不一致和次优学习。
method: 提出MBDPO，将策略优化转化为潜在世界模型中搜索轨迹上的扩散过程，提取隐式能量函数锚定策略并优化分数场。
result: 在多任务离线预训练、在线学习及微调中表现优异，离线大规模预训练随容量增大性能单调提升。
conclusion: 通过扩散策略表示统一搜索与优化，有效缓解错配，释放世界模型规模扩展潜力。
---

## 摘要
基于模型的强化学习可以通过使用世界模型在规模上得到有效支持。然而，在实践中，扩展此类方法仍然受到根本限制。一个普遍认可的挑战是模型偏差和误差累积，这降低了长期预测的质量。除了这些问题，我们识别出一个更关键但尚未充分探索的瓶颈：现有世界模型方法中搜索与价值学习之间的结构错位。具体而言，策略改进通常依赖于由独立非搜索策略诱导的价值函数，导致训练不一致并最终产生次优学习。为解决这一局限，我们提出在世界模型中的基于模型的扩散策略优化（MBDPO），一个通过扩散策略表示统一搜索与策略优化的框架，从而释放世界模型在可扩展策略学习中的潜力。我们不在学习的世界模型上构建显式规划器，而是将策略优化重新表述为潜在世界模型中搜索轨迹上的扩散过程。据此，我们从收集的数据集中提取一个隐式能量函数来锚定策略，使MBDPO能够优化策略的得分场，同时缓解错位问题。我们在多任务离线预训练、在线学习以及离线到在线微调等多种设置下评估MBDPO。在离线场景中，我们进一步通过在大规模数据集上预训练来研究其缩放行为，观察到随着模型容量增加，性能持续且单调提升。

## Abstract
Model-based reinforcement learning (RL) can be effectively supported at scale through the use of world models. However, in practice, scaling such approaches remains fundamentally limited. A commonly recognized challenge is model bias and error compounding, which degrade long-horizon predictions. Beyond these issues, we identify a more critical yet underexplored bottleneck: a structural misalignment between search and value learning in existing world model approaches. In particular, policy improvement often relies on value functions induced by a separate, non-search policy, resulting in training inconsistency and ultimately suboptimal learning. To address this limitation, we propose Model-Based Diffusion Policy Optimization (MBDPO) in world models, a framework that unifies search and policy optimization through diffusion policy representations, thereby unlocking the potential of world models for scalable policy learning. Instead of constructing an explicit planner over a learned world model, we reformulate policy optimization as a diffusion process over searched trajectories in latent world models. In this view, we extract an implicit energy function from the collected dataset that anchors the policy, enabling MBDPO to refine the score field for policy optimization while mitigating misalignment. We evaluate MBDPO across a wide range of settings, including multi-task offline pretraining, online learning, and offline-to-online fine-tuning. In the offline regime, we further investigate its scaling behavior by pretraining on large-scale datasets, observing consistent and monotonic performance gains with increasing model capacity.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有基于世界模型的强化学习方法（如TD-MPC系列）在扩展时面临根本限制。传统认知认为主要瓶颈是模型偏差和长期预测误差累积，但本文指出更关键的是**搜索与价值学习的结构性错位**：策略改进依赖于由独立非搜索策略（如策略网络）训练的价值函数，而实际执行和优化却依赖基于搜索程序（如MPPI）产生的轨迹。这种不匹配导致价值函数被查询到分布外（OOD）的状态-动作对，产生不可靠的乐观估计，搜索程序进而利用这些错误，形成系统性高估偏差，损害性能和可扩展性。

- **整体含义**：论文旨在通过扩散策略表示统一搜索与策略优化，消除错位，释放世界模型在可扩展策略学习中的潜力。核心假设是扩散策略可以自然地桥接世界模型与策略优化，通过隐式能量函数约束策略更新在信任区域内，实现单调的策略改进。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 将策略优化重新表述为潜在世界模型中搜索轨迹上的扩散过程。使用去噪扩散概率模型（DDPM）表示生成策略，通过世界模型评估的回报来引导分数匹配，从而定义逆向扩散过程。
- 从缓冲区数据集中学习隐式能量函数 \( E_\theta \)，其与行为策略 \(\beta\) 的密度近似（\(\log \beta\)），并将其吸收到能量正则化回报 \(\tilde{G}\) 中，从而将KL散度约束自然融入优化目标。

### 关键技术细节
1. **世界模型**：包含编码器 \(E\)、潜在动力学 \(F\)、潜在奖励 \(R\)，全部在潜在空间 \(Z\) 中运行，支持多任务通过可学习任务嵌入 \(e\)。
2. **扩散策略**：使用DDPM表示动作序列策略 \(\pi_\phi(a_{t:t+H}|z_t)\)，通过逆向扩散过程逐步从高斯噪声生成优化动作序列。得分函数 \(\phi\) 通过世界模型生成的想象轨迹估计：
   - 从后验分布 \(q(\cdot|a^\tau_{t:t+H}, z_t)\) 采样候选清洁动作序列。
   - 利用世界模型滚动出潜在轨迹，计算能量正则化累计回报 \(\tilde{G}\)。
   - 使用重要性权重 \(w\) 更新得分函数，得到 \(\phi\) 的蒙特卡洛估计。
3. **隐式能量函数**：通过对比损失从缓冲区学习 \(E_\theta\)，使得 \(-E(z,a) \approx \log \beta(a|z) + const\)，从而将KL约束（式8）转化为奖励项中的能量正则化。
4. **联合训练**：交替优化世界模型（编码器、动力学、奖励、价值函数 \(Q\)、能量函数）和扩散策略（得分网络 \(\hat{\phi}\)）。得分网络训练的损失为 \( \min \mathbb{E}[\|\hat{\phi} - \phi\|^2] \)。
5. **策略执行**：训练后，推理时直接使用得分网络进行快速逆向扩散采样，无需在线蒙特卡洛。

### 关键公式（用文字说明）
- KL约束优化目标（式8）：最大化期望回报，同时约束策略与行为策略的KL散度不超过 \(\epsilon\)，且遵循潜在动力学。
- 隐式能量吸收后，奖励变为 \(\tilde{R}(z_h, a_h) = R(z_h, a_h) - \eta \gamma^{h-t} E(z_h, a_h)\)。
- 得分函数（式10）定义为：从后验分布采样清洁动作，计算其能量正则化回报，以softmax权重加权平均，再转化为得分。
- 定理1：证明得分匹配条件等价于KL约束下的最优Gibbs策略，从而统一搜索与优化。

## 3. 实验设计

### 数据集/场景
- **Benchmarks**：四个主要基准，共121个多样化控制任务：
  - DMControl（39个任务）
  - MetaWorld（50个任务）
  - ManiSkill2（5个任务）
  - MyoSuite（10个任务）
  - 额外：Visual RL（图像输入，10个任务）、Locomotion（7个任务）等子集。
- **训练范式**：
  - 多任务离线预训练（80任务和30任务数据集）
  - 在线从头训练（from scratch）
  - 离线到在线微调（O2O）：先在70个任务上预训练，再在10个未见任务上微调，每个任务仅40k交互。

### 对比方法
- SAC（模型无关强化学习）
- DreamerV3（基于世界模型）
- TD-MPC（TD-MPC系列基础）
- TD-MPC2（最先进世界模型方法）

### 实验充分性
- 在线训练：5个领域共121个任务，每个任务3个随机种子，绘制学习曲线。
- 离线预训练：模型规模从1.7M到340M，评估两个数据集（80任务和30任务），报告归一化分数。
- 消融实验：
  - 正则化因子 \(\eta\)（0~5）
  - 扩散去噪步数 \(N\)（5~20）
  - 蒙特卡洛样本数 \(T\)（64~1024）
  - 动作漂移与交叉TD误差分析
- 潜变量轨迹可视化（单集和多集，共80个任务）。
- 训练时间与评估时间详细分析（每个任务表）。
- 综合评价充分，覆盖了不同规模、不同任务类型的性能对比和理论验证。

## 4. 资源与算力

- 论文中提供了训练时间表（Table 1 和 Table 2,3）：
  - 在线训练：在单个NVIDIA A800-80GB GPU上测量，每环境步数毫秒级（MetaWorld约1.08ms，DMControl约0.76ms等）。
  - 离线训练：10M训练步在单个NVIDIA-H200 GPU上，时间从34.8h（1.7M参数80任务）到202.9h（340M参数30任务）。
  - 评估时间：每个评估阶段（10个episode）时间在10~20秒（MetaWorld）到48~116秒（DMControl）。
- 未明确说明使用的GPU总数，但显示在单个或少量GPU上完成，算力开销合理。

## 5. 实验数量与充分性

- 实验数量巨大：在线121个任务+离线多规模预训练+消融+可视化+计算分析，覆盖广泛。
- 每个实验均采用多个随机种子，报告均值和标准差，确保了统计显著性。
- 对比方法覆盖领域内最先进（SAC, DreamerV3, TD-MPC, TD-MPC2）。
- 消融实验系统探索了关键超参数的影响。
- 可视化展示潜变量轨迹，验证物理直觉。
- 总体实验设计充分、客观、公平，进行了控制变量和基线比较。

## 6. 论文的主要结论与发现

1. **性能优势**：MBDPO在所有基准上一致优于或竞争力于基线方法，尤其在线从头训练中样本效率更高、方差更低。
2. **可扩展性**：多任务离线预训练中，MBDPO随模型容量（1.7M→340M）呈现单调且更陡峭的性能提升，而基线TD-MPC2提升更平缓且在小模型上表现较差。
3. **克服错位**：MBDPO显著降低交叉TD误差和动作漂移，验证了其有效解决了搜索-价值错位问题（Problem 1, 2）。
4. **迁移与微调**：离线到在线微调中，预训练的通用智能体可在少量交互下（40k）快速适应新任务，远优于从头训练。
5. **潜变量表示**：MBDPO学习到物理上有意义的潜变量轨迹（闭环、平滑目标导向），而TD-MPC2轨迹更混乱。

## 7. 优点

- **理论深度**：系统分析了现有方法的错位瓶颈，并给出数学证明（Problem 1, 2、Theorem 1）。
- **统一框架**：将搜索与策略优化统一为扩散过程，通过隐式能量自然引入KL约束，避免了启发式搜索的OOD问题。
- **广泛评估**：覆盖121个任务、多种训练范式、不同模型规模，结果全面。
- **可解释性**：通过潜变量轨迹可视化展示学习到的结构化行为，增强了方法的可理解性。
- **计算效率**：在线训练时间与TD-MPC2相当，部分领域更快；离线扩展开销增长可控（表1）。

## 8. 不足与局限

- **计算资源限制**：论文提到未扩展到极端大参数（如基础模型规模），受限于有限算力，全规模极限未探索。
- **模拟环境局限**：目前仅在仿真环境中评估，尚未在真实机器人上部署，实际转移性待验证。
- **任务特异性失败**：如“Hopper Hop”任务由于潜变量表示分布严重偏离（如图23所示），性能显著低于其他任务，表明对表示质量敏感。
- **扩散采样开销**：虽然训练后推理快，但训练阶段需要蒙特卡洛采样，可能在大规模离线预训练中增加时间（如340M模型需约200小时）。
- **超参数敏感性**：消融显示 \(\eta\) 过大（≥5）显著降低性能，需要调参。
- **对比范围**：未与最新模型无关扩散策略（如Diffuser、Q-score matching）进行对比，仅与基于搜索的世界模型方法对比。

（完）
