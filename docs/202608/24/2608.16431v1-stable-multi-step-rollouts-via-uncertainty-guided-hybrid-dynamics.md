---
title: Stable Multi-Step Rollouts via Uncertainty-Guided Hybrid Dynamics
title_zh: 基于不确定性引导的混合动力学的稳定多步推演
authors: "Andrei Maalberg, Axel Neumann, Jens Knobloch"
date: 2026-08-17
pdf: "https://arxiv.org/pdf/2608.16431v1"
tags: ["query:mbrl-manip"]
score: 8.0
evidence: 通过学习动力学模型实现稳定多步展开的模型强化学习
tldr: 学得的动力学模型在多步递归预测中常因累积误差而发散，导致模型基RL策略更新不可靠。本文提出不确定性引导的混合动力学框架，将可证明收缩的名义模型与灵活的越界模型通过校准认知不确定性切换，使各模型只在自身可信区间内工作。该混合预测器保证递归轨迹全局有界，名义区稳定、越界时仿射增长。在Duffing振荡器上的模型基RL实验验证了稳定长时预测和更优的成本-努力权衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.16431v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 764, \"height\": 963, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.16431v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 762, \"height\": 810, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.16431v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 756, \"height\": 408, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.16431v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 905, \"height\": 422, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.16431v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 738, \"height\": 293, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.16431v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 865, \"height\": 197, \"label\": \"Table\"}]"
motivation: 学得的动力学模型在多步rollout中递归应用时不稳定，产生发散轨迹并破坏策略更新。
method: 基于校准认知不确定性作为切换信号，在可证明收缩的名义模型与灵活模型间切换，使各模型仅在可靠性区间内工作。
result: 混合预测器保证递归多步rollout全局有界，名义区内Lyapunov稳定、越界时仿射增长，Duffing实验显示稳定预测。
conclusion: 不确定性引导的混合动力学提供模型无关的稳定预测框架，改善了模型基RL预测控制的成本与努力权衡。
---

## 摘要
多步推演对于基于模型的强化学习（RL）和预测控制至关重要，然而学习到的动力学模型在递归应用时常常变得不稳定，导致发散和不可靠的策略更新。本文提出了一种模型无关的混合动力学框架，该框架通过不确定性引导的切换律，将可证明收缩的名义模型与灵活的偏移模型融合在一起。切换信号源自经校准的认知不确定性，并且仅在系统离开名义区域时激活，从而确保每个模型都在其可靠区域内运行。在明确陈述的光滑性和有界性假设下，我们证明了所得到的混合预测器产生全局有界的递归多步推演：轨迹在名义区域内保持李雅普诺夫稳定，在偏移期间最多呈现仿射增长。为了在实践中说明该理论，我们在一个基于模型的RL方案中实例化了混合动力学框架，该方案使用真实的一步转移进行价值学习，并使用混合推演进行策略改进。在非线性Duffing振荡器上的实验表明，与稳定基线相比，该方法实现了稳定的长时域预测和更好的成本-努力权衡。

## Abstract
Multi-step rollouts are essential for model-based reinforcement learning (RL) and predictive control, yet learned dynamics models often become unstable when recursively applied, leading to divergence and unreliable policy updates. This paper proposes a model-agnostic hybrid dynamics framework that blends a provably contracting nominal model with a flexible excursion model through an uncertainty-guided switching law. The switching signal is derived from calibrated epistemic uncertainty and activates only when the system leaves the nominal region, ensuring that each model operates within its reliability regime. Under clearly stated smoothness and boundedness assumptions, we show that the resulting hybrid predictor yields globally bounded recursive multi-step rollouts: trajectories remain Lyapunov-stable in the nominal region and exhibit at most affine growth during excursions. To illustrate the theory in practice, we instantiate the hybrid dynamics framework within a model-based RL scheme that uses real one-step transitions for value learning and hybrid rollouts for policy improvement. Experiments on a nonlinear Duffing oscillator demonstrate stable long-horizon prediction and improved cost-effort trade-offs relative to a stabilizing baseline.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义

- **研究动机**：在基于模型的强化学习（Model-based RL）和预测控制中，多步推演（Multi-step rollout）是关键环节——模型需要被递归地应用于未来多个时间步来预测轨迹、评估策略或进行规划。然而，学习得到的动力学模型在递归应用时，单步预测误差会逐步累积和放大（即复合误差，compounding error），导致轨迹发散和漂移，进而产生不可靠的策略更新。这种不稳定性的本质在于：学习模型缺乏收缩性（contractivity）、谱性质或分布外（OOD）行为方面的保证。
- **现有方法的不足**：
  - 直接训练多步预测器（而非递归迭代单步模型）可以减少统计偏差，但回避了递归应用下的稳定性问题，且不适用于控制场景中固有的迭代结构。
  - 经典控制理论通过线性状态空间近似或Koopman嵌入施加结构约束，可获得可分析的长时域行为，但缺乏表达力来捕捉非线性瞬态或随时间变化的动力学。
  - 实际系统中，动力学常常在名义区域（简单线性模型即可）和偏移区域（非线性效应主导）之间切换，单一模型难以兼顾两方面的需求。
- **论文核心思路**：提出一个**模型无关的混合动力学框架**，在（i）可证明收缩的名义模型和（ii）有界增长的偏移模型之间，通过基于校准认知不确定性（calibrated epistemic uncertainty）的切换律进行融合。当名义模型的不确定性低时，系统保持在收缩状态；当不确定性升高时，系统切换至偏移模型来处理非线性瞬态。这种机制构成一种“自调节”的学习动力学体系，确保递归多步推演整体有界且稳定。
- **整体意义**：为基于模型的RL和预测控制中一个长期困扰的核心问题——学习模型在递归推演中的不稳定性——提供了理论上有保证的解决方案，并为后续将稳定长时域推演用于策略改进提供了可行路径。

### 2. 论文提出的方法论

**（1）核心思想：不确定性引导的混合预测器**

论文提出了一个离散时间混合系统模型，状态更新形式为：

```
x_{t+1} = α_t · f_nom(x_t, u_t) + (1 - α_t) · f_exc(x_t, u_t)
```

其中：

- `f_nom`：名义模型（可证明收缩的线性/Koopman型预测器）
- `f_exc`：偏移模型（具有有界增长的非线性瞬态预测器）
- `α_t`：混合系数，由单调函数作用于不确定性估计而得：`α_t = σ(ζ(x_t, u_t)) ∈ [0,1]`
- **切换规则**：当不确定性 `ζ ≤ ζ⋆` 时，`α = 1`（完全信任名义模型）；当不确定性 `ζ > ζ⋆` 时，`α < 1`（偏移模型介入）

**（2）关键假设（理论前提）**

| 编号 | 假设 | 含义 |
|------|------|------|
| 假设1 | 名义模型收缩性 | 在李雅普诺夫函数下，名义模型在置信区域内满足严格的二次型递减 |
| 假设2 | 偏移模型有界增长 | 偏移模型至少满足线性增长上界：`‖f_exc(x,u)‖ ≤ M(1+‖x‖)` |
| 假设3 | 不确定性校准误差 | 建模误差被不确定性函数上界约束：`‖e(x,u)‖ ≤ δ(ζ(x,u))` |
| 假设4 | 有限偏移恢复 | 每次偏移持续时间有上限 H，且之后必然恢复到名义置信区域 |
| 假设5 | 有界增益控制策略 | 控制输入满足 `‖u_t‖ ≤ c_u‖x_t‖ + c_0`（如LQR+有界残差策略） |

**（3）核心理论结果**

- **引理1（实际名义收缩）**：在名义置信区域内，真正的动力学满足 `V(x_{t+1}) - V(x_t) ≤ -c_3‖x_t‖² + ε`，即在一个紧致邻域之外保证严格李雅普诺夫递减。
- **引理2（有限时域有界性）**：在任意 k ≤ H 步偏移期间，状态范数满足 `‖x_{t+k}‖ ≤ M^k‖x_t‖ + M(M^k - 1)/(M - 1)`，即偏移造成的增长是均匀有界的。
- **定理1（全局有界性）**：在假设1–5下，混合系统的所有轨迹满足 `sup_t ‖x_t‖ < ∞`，即全局有界。名义阶段收缩，偏移阶段增长率有界且持续时间有限，切换拼接不会导致累积无界增长。

**（4）KIND-RL实例化（应用层）**

- **名义模型**：Koopman/DMD类局部线性算子，仅在分布内（ID）数据上训练。
- **偏移模型**：非线性Transformer，仅在分布外（OOD）段（冲击、碰撞、极限环振荡等）训练。
- **不确定性估计**：每个预测器关联一个学习的不确定性模块，用于预测其一步建模误差的大小；运行时无需真实下一状态即可产生置信度信号。
- **切换实现**：使用不确定性归一化加权 `α_t = ζ_exc/(ζ_exc + ζ_nom)`，使低不确定性模型获得更高权重，无需手动调整阈值。
- **控制策略**：`u_t = u_LQR + Δu_RL`，即在LQR稳定基线上叠加有界残差策略 `‖Δu_RL‖ ≤ u_max`，满足假设5。
- **训练流程**：价值函数只用真实一步转移进行TD学习；策略更新则使用混合预测器的H步推演来计算优势估计，从而在避免模型偏差污染价值估计的同时，实现长时域信用分配。

### 3. 实验设计

- **实验场景**：受控的非线性Duffing振荡器，参数为：线性刚度 μ=110.0、非线性刚度 β=140.0、驱动力幅值 ρ=70.0、阻尼 κ=0.5、驱动力频率 ω=1.2、离散化步长 Δt=0.01。控制目标是调节至参考点 x⋆ = [1, 0]ᵀ。
- **Benchmark与对比**：
  - **基线**：围绕参考点线性化设计的LQR控制器（增益经过扰动以引入模型失配，不作为性能基准，而是提供稳定基线和名义控制信号）。
  - **对比方法**：LQR基线 vs. LQR + RL残差策略（H=2和H=200两种推演时域）。
- **实验内容**：
  - 实验一：长时域混合推演稳定性验证（600步推演，每20步重新锚定到真实状态），检验预测轨迹的有界性、不确定性信号变化和混合系数调整行为。
  - 实验二：RL策略改进验证，对比LQR基线与叠加残差策略后的系统行为（参考井停留时间和控制振幅变化）。
  - 实验三：推演时域影响分析，比较H=2和H=200时的优势信号和平滑度。
  - 结果指标：累计成本（cost）、控制努力（effort）和参考井驻留时间（dwell）。

### 4. 资源与算力

- **论文中未明确说明**所使用的GPU型号、数量或训练时长等信息。
- 文中仅提及KIND预测器在实验中保持固定（frozen），RL训练过程使用了5个随机种子；未提及任何关于计算资源（如处理器类型、训练时间、能耗）的量化描述。

### 5. 实验数量与充分性

- **实验组数**：主要实验包括三类（长时域推演稳定性、策略改进行为、推演时域影响），每种实验对比了2–3个配置（LQR baseline、H=2、H=200），另有5个随机种子的统计结果。
- **充分性评估**：
  - **优点**：理论结果与实验验证之间有清晰的对应关系；控制变量设计合理（固定KIND预测器，只变化RL训练过程和推演时域）；5个随机种子提供了基本统计可信度。
  - **不足**：
    - 仅使用单一实验场景（Duffing振荡器），缺乏多场景、多任务验证，难以证明方法的普遍适用性。
    - 没有进行消融研究（如移除不确定性切换、仅用名义模型、仅用偏移模型、切换阈值敏感性等）。
    - 缺乏与现有模型基RL方法（如MBPO等）的直接对比。
    - 高维、部分可观测、随机性更强的系统未经测试。
- **总体评价**：实验作为理论证明的补充说明是足够的，但作为全面的实证验证，覆盖度有限。

### 6. 论文的主要结论与发现

- **理论层面**：不确定性引导的混合动力学框架在明确的假设条件下，可以保证递归多步推演的**全局有界性**——名义阶段满足李雅普诺夫稳定性，偏移阶段增长至多仿射且有界持续时间，二者拼接不会导致发散。
- **实验层面**：
  - 混合预测器在600步推演中保持有界，能够跟踪Duffing振子的井间跃迁（inter-well transitions）而不会发散。
  - 名义不确定性在名义区低、在跃迁时尖峰、回归名义区时恢复，验证了区域分离机制的有效性。
  - RL残差策略相比LQR基线降低了累计成本和控制努力，且长推演时域（H=200）比短推演（H=2）产生更平滑的优势信号和更小的策略更新方差。

### 7. 优点

- **理论贡献扎实**：将多步推演稳定性这一实际问题形式化为混合系统问题，给出清晰的假设条件和严格的李雅普诺夫/Lyapunov + 有界增长证明，这在现有文献中较为罕见。
- **模型无关性**：理论框架适用于任何满足假设的模型对，不依赖于特定的神经网络架构或KIND实现，普适性强。
- **不确定性作为切换信号的新颖应用**：已有工作将不确定性用于安全过滤、反馈增益调整或数据采集，本文将其用于**调节模型类别本身**，这是概念上的创新。
- **理论-实践闭环**：从抽象理论到具体实现（KIND架构）再到实验验证，每一步都有清晰对应关系（表I），便于读者理解和复现。
- **方法的设计匠心**：不确定性归一化混合系数（α）自然实现了“信任低不确定性模型”的逻辑，无需手动调阈值，且对个别模型失效具有鲁棒性（偏移模型在ID区域不确定性高时自动被抑制）。
- **价值学习与策略改进分离**：只用真实数据做价值学习、只用模型推演做策略改进，避免模型偏差污染价值估计，是一种深思熟虑的设计选择。
- **实验与理论一致**：图1中不确定性信号在井间跃迁时尖峰、α相应下降，直观验证了理论模型中的切换行为。

### 8. 不足与局限

- **实验场景单一**：仅Duffing振荡器一个benchmark，缺乏多领域验证（如机器人控制、自动驾驶、射频系统等），方法的普适性缺少实证支撑。
- **理想化假设的限制**：
  - 假设3（不确定性校准）依赖于学习的不确定性估计器是否真正上界建模误差，论文承认这是一种理想化；虽然有界校准误差 η 下结果仍定性成立，但 η 的大小和实际校准方式缺乏讨论。
  - 假设4（有限偏移恢复）是“操作假设”而非由构造保证的；在实际系统中，偏移是否总能被闭环策略消除取决于控制器的能力。
- **计算细节缺失**：未提供网络架构的具体规模、训练数据量、超参数设置、训练时长等复现所需的关键实现信息。
- **缺乏与现有方法的公平对比**：没有与常见模型基RL算法（如MBPO、PETS）或纯非线性模型（如Transformer直接预测）进行系统比较。
- **轨迹重新锚定依赖**：实验中每20步将上下文窗口重新锚定到真实状态，这是一种“作弊”式的外部干预，降低了纯递归推演可信度的说服力；虽然这是模型基RL中的常见做法，但论文并未单独评估锚定频率对稳定性的影响。
- **总成本函数与Duffing行为的耦合限制**：LQR被设计为“故意不抑制井间跃迁”，这有利于展示切换机制，但也意味着实验场景可能被特殊构造以适应方法的特性。
- **随机性来源有限**：论文中仅RL训练过程引入了随机性，KIND预测器固定；这回避了模型训练随机性对推演稳定性的影响问题。

---

（完）
