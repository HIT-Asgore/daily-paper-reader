---
title: "Neural Control: Adjoint Learning Through Equilibrium Constraints"
title_zh: 神经控制：通过平衡约束的伴随学习
authors: "Dezhong Tong, Jiawen Wang, Hengyi Zhou, Yinglong Shen, Xiaonan Huang, M. Khalid Jawed"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03288v1"
tags: ["query:diff-physics"]
score: 8.0
evidence: 提出Neural Control框架，通过平衡约束的伴随学习实现隐式系统的可微控制
tldr: 在物理AI任务中，可变形物体的控制常涉及隐式平衡约束与多稳态非线性，但传统通过展开求解器反向传播的方法计算与内存开销大。本文提出Neural Control框架，利用伴随公式对平衡条件求导，无需展开迭代求解器即可计算轨迹相关的代理梯度，并融入滚动时域MPC以增强多稳态下的鲁棒性。在仿真与实物机器人操作可变形线性物体（DLO）实验中，该方法相比SPSA、CEM等无梯度方法取得了更优的控制性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.03288v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 840, \"height\": 449, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.03288v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1775, \"height\": 599, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.03288v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 856, \"height\": 527, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.03288v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1759, \"height\": 363, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.03288v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1755, \"height\": 699, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.03288v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 776, \"height\": 581, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.03288v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1785, \"height\": 157, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.03288v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 861, \"height\": 1985, \"label\": \"Table\"}]"
motivation: 现有方法在隐式平衡约束下反向传播需展开迭代求解器，计算与内存成本高，且多稳态导致控制脆弱。
method: 提出Neural Control框架，通过伴随法对平衡条件求导获得代理梯度，无需展开求解器；结合MPC反复锚定已实现平衡，提升长时域鲁棒性。
result: 在仿真与实物DLO操控中，Neural Control的轨迹跟踪误差显著低于SPSA和CEM基线。
conclusion: Neural Control实现了高效、鲁棒的非线性隐式平衡控制，适用于多稳态物理系统。
---

## 摘要
许多物理AI任务受隐式平衡支配：智能体驱动一部分自由度（边界DoFs），而其余自由自由度通过最小化总势能达到平衡。即使像将可变形线性物体（DLO）弯曲到目标形状这样看似简单的任务，由于多稳定性也可能表现出强非线性行为：相同的边界条件可能根据驱动轨迹产生多个平衡形状。然而，此类系统中的学习和控制是脆弱的，因为驱动到构型的映射仅隐式定义，而通过迭代平衡求解器的朴素反向传播是内存和计算密集型的。我们提出神经控制，一种边界控制框架，通过伴随公式区分平衡条件来计算轨迹相关、内存高效的代理梯度，避免展开求解器迭代。为了提高长时域鲁棒性，我们将这些敏感性集成到滚动时域MPC方案中，该方案反复将优化重新锚定到实现的平衡，并减轻多稳定状态下的盆切换。我们在仿真和物理机器人操纵DLO上评估神经控制，并显示出优于梯度无关基线（如SPSA和CEM）的性能。

## Abstract
Many physical AI tasks are governed by implicit equilibrium: an agent actuates a subset of degrees of freedom (boundary DoFs), while the remaining free DoFs settle by minimizing a total potential energy. Even seemingly basic tasks such as bending a deformable linear object (DLO) to a target shape can exhibit strongly nonlinear behavior due to multi-stability: the same boundary conditions may yield multiple equilibrium shapes depending on the actuation trajectory. However, learning and control in such systems is brittle because the actuation-to-configuration map is defined only implicitly, and naive backpropagation through iterative equilibrium solvers is memory- and compute-intensive. We propose Neural Control, a boundary-control framework that computes trajectory-dependent, memory-efficient proxy gradients by differentiating equilibrium conditions via an adjoint formulation, avoiding unrolling of solver iterations. To improve robustness over long horizons, we integrate these sensitivities into a receding-horizon MPC scheme that repeatedly re-anchors optimization to realized equilibria and mitigates basin-switching in multi-stable regimes. We evaluate Neural Control in simulation and on physical robots manipulating DLOs, and show improved performance over gradient-free baselines such as SPSA and CEM.

---

## 论文详细总结（自动生成）

# 论文中文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：许多物理AI任务（如机器人操控可变形线性物体DLO）由隐式平衡约束支配：边界自由度被驱动，而内部自由度通过最小化总势能达到平衡。这类系统具有强非线性和多稳定性——相同的边界条件可能因驱动轨迹不同而产生多个稳定平衡形状。这导致两个挑战：(i) 状态更新仅隐式定义为平衡问题的解，通过迭代求解器（如Newton-CG）进行朴素反向传播（BPTT）计算和内存开销巨大；(ii) 长时域下多稳定态导致难以保持分支一致性，小误差可能引发不期望的盆切换。
- **研究动机**：现有方法如SPSA、CEM等无梯度优化收敛慢且计算成本高；可微仿真虽能提供梯度，但通过求解器迭代展开反向传播在长时域下数值脆弱且内存密集。因此需要一种高效、分支一致的梯度计算方法。
- **整体含义**：本文提出**Neural Control**框架，通过伴随公式在隐式平衡条件下计算代理梯度，无需展开求解器迭代，并结合滚动时域（Receding-Horizon）MPC来提高多稳定态下的鲁棒性。该方法在仿真和实物机器人操控弹性带的实验中，相比SPSA、CEM等基线实现了显著更优的性能和效率。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：
  - 利用伴随敏感性方法（Adjoint sensitivity），通过对平衡条件进行隐式微分得到局部敏感性，而非通过展开求解器迭代反向传播。
  - 提出“冻结切线（frozen-tangent）”近似，忽略敏感性矩阵沿轨迹的变化（即忽略 \(dS/d\lambda\) 和高阶项），从而避免高阶导数，确保梯度计算在理论上具有一阶精度。
  - 结合滚动时域（RHC）将长时域优化分解为多个短段，每段重新锚定到已实现的平衡构型，减少代理误差积累和盆切换风险。

- **关键技术细节**：
  - **系统建模**：状态 \(x(\lambda)\)（自由节点）通过求解平衡方程 \(G(x(\lambda), z(\lambda))=0\) 隐式定义，其中 \(z(\lambda)\) 为可控边界条件。控制量 \(u(\lambda)\) 通过续贯动力学 \(dz/d\lambda = f(z(\lambda), u(\lambda))\) 驱动 \(z\)。
  - **代理敏感性**：局部敏感性 \(S(\lambda) := \partial x / \partial z\) 满足 \(G_x S = -G_z\)。不显式形成 \(S\)，而是通过一次伴随线性系统 \(G_x^\top p = v\) 计算 \(S^\top v\)。
  - **代理伴随递推**：利用离散后的代理过渡方程 \(x_{k+1} \approx x_k + \Delta\lambda S_k f(z_k, u_k)\) 进行反向传播，推导出终端和轨迹目标下的伴随方程与策略梯度公式（如公式(10)-(16)）。该代理梯度仅用隐式敏感性，无需展开求解器。
  - **滚动时域（RHC）**：将续贯过程分为多段（每段 \(H\) 步），每段内优化轻量神经网络控制器参数 \(\Theta^{(m)}\)，执行后以段末状态作为下一段起点，重复直至完成全任务。算法1给出了完整流程。

- **近似严格性**：定理C.3指出，在假设敏感性平滑变化（\(\|dS/d\lambda\|\le\varepsilon\)）下，代理梯度与真实梯度的误差为 \(O(\varepsilon)\)，即一阶精确。

## 3. 实验设计：数据集/场景、基准、对比方法

- **实验场景**：弹性带（elastic strip）操控，模拟实际机器人操控可变形线性物体。弹性带使用离散微分几何（DDG）模型，包含拉伸和弯曲能量，忽略重力（除非特殊说明）。离散化为101个节点，两端节点作为边界自由度（\(z\)），中间节点为自由自由度（\(x\)）。
- **三个任务（逐步增加难度）**：
  - **任务1（点对点节点定位）**：驱动选定的节点到目标位置（终端损失）。
  - **任务2（轨迹跟踪）**：驱动中间节点跟踪预定平面路径（正弦、圆、方波等），使用轨迹损失。
  - **任务3（多稳态形状形成）**：通过单端夹持操控弹性带形成目标形状（曲率匹配损失），存在多稳态（如C形、U形、M形）。
- **对比方法**：
  - SPSA（同时扰动随机逼近）
  - CEM（交叉熵方法）
  - Adjoint-only（代理梯度无RHC，全时域）
  - Adjoint+RHC（完整方法）
- **评估指标**：最佳损失（best loss）、总计算时间、收敛曲线。所有方法共享相同的前向模拟器（每次迭代都求解平衡到收敛）和控制器架构（MLP输入\(\lambda\)输出控制量），仅在优化器上不同。

## 4. 资源与算力

- **文中明确说明**：未给出具体的GPU型号、数量或训练时长信息。仅报告了相比SPSA/CEM的**效率优势**：例如Adjoint+RHC在任务1–3中运行速度是SPSA的19–127倍，同时达到更低损失。文中提到“wall-clock time”包括所有平衡求解和线性系统开销，但未注明硬件细节。
- **推测**：实验可能使用单个GPU（如NVIDIA RTX系列）或CPU进行仿真，因为模型规模不大（101节点），但论文未明确说明。

## 5. 实验数量与充分性

- **实验数量**：
  - 数值实验：三个任务各包含多个子案例（任务1：4个代表案例；任务2：4种轨迹（正弦、圆、方波、三角波）；任务3：多稳态形状形成（展示多种目标形状）。
  - 消融实验：完整比较了四种方法（SPSA、CEM、Adjoint-only、Adjoint+RHC）在所有任务上的表现，并报告了均值与标准差（图4、表1）。
  - 实物实验：使用6-DOF机械臂操控弹性带，展示了三个任务（点对点、轨迹跟踪、形状控制）的定性结果，并进行了简单的系统辨识（通过重力-弯曲长度 \(L_{gb}\) 校准）。
- **充分性与客观性**：
  - 数值实验覆盖了不同难度（终端、轨迹、多稳态），对比方法包括主流无梯度基线（SPSA、CEM），并且进行了消融（去掉RHC）。
  - 所有方法在同一更新预算（参数更新步数\(T\)）下比较，且考虑了不同方法每步需要的评估次数（SPSA需2次，CEM需\(P\)次，Adjoint只需1次），报告了总时间，较为公平。
  - 实物实验提供了定性验证，但未给出定量误差对比，可能存在一定偏差（如系统辨识误差、重力未完全补偿等）。
- **不足**：缺少与基于可微仿真（如BPTT展开Newton迭代）的直接对比，未分析在更多物理系统（如3D变形体、接触）上的泛化性。

## 6. 论文的主要结论与发现

- **Neural Control（Adjoint+RHC）在所有任务上均显著优于无梯度基线**：达到更低的最终损失（1–6个数量级更低），同时计算时间减少19–127倍。
- **RHC是关键组件**：在长时域（任务2）和多稳态（任务3）下，Adjoint-only因代理误差累积表现较差，而Adjoint+RHC通过段式重锚保持了分支一致性和鲁棒性。
- **代理梯度方向足够有效**：尽管使用了冻结切线近似，实际优化仍能成功收敛，表明该近似在平滑分支下是实用的。
- **从仿真到实物迁移可行**：在真实机器人上成功复现了三个任务，显示方法对模型失配和重力的鲁棒性。

## 7. 优点：方法或实验设计上的亮点

- **方法论创新**：
  - 首次将伴随敏感性方法与隐式平衡求解器结合，提出无需展开求解器迭代的代理梯度，大幅降低内存和计算成本。
  - 冻结切线近似与滚动时域配合，在保持梯度实用性的同时提升了长时域稳定性。
  - 统一了终端和轨迹目标的伴随公式，通用性强。
- **实验设计亮点**：
  - 逐步增加难度的三个任务设计，清晰展示了方法的鲁棒性和扩展性。
  - 公平比较：所有方法共享前向模拟器和控制器，仅优化器不同；对比时同时报告损失和时间，反映真实效率。
  - 实物实验验证了从仿真到实际的迁移能力，增加了工作的实用性。
- **理论分析**：提供了代理梯度误差的一阶精确性证明（附录C），增强了方法的可信度。

## 8. 不足与局限

- **理论近似局限**：冻结切线假设敏感性变化缓慢（\(\|dS/d\lambda\|\le\varepsilon\)），在接近分岔点或非光滑物理时可能失效，导致梯度方向不准确。
- **实验覆盖有限**：
  - 仅测试了弹性带（平面DLO）操控，未拓展到3D变形体、接触或摩擦场景。
  - 未与基于可微仿真（如BPTT展开Newton迭代）的方法直接对比，尽管文中提到此类方法内存和数值脆弱，但未给出定量比较。
  - 实物实验仅为定性展示，缺乏定量误差指标（如跟踪精度）。
- **算法限制**：
  - 平衡求解器仍是主要成本（\(C_{eq}\)），每次前向都需要收敛到高精度，限制了整体速度。
  - RHC中段内控制器参数被重新初始化并每段优化，可能忽略跨段的长时记忆，对于非常复杂的任务可能需要额外的终端成本或记忆机制。
- **资源报告不充分**：未明确硬件配置和具体训练时长，可重复性略有不足。

（完）
