---
title: Accelerating Visual Policy Learning with Sampling-Based Model Predictive Control
title_zh: 利用基于采样的模型预测控制加速视觉策略学习
authors: "Yilang Liu, Haoxiang You, Qian Wang, Daniel Rakita, Ian Abraham"
date: 2026-09-17
pdf: "https://arxiv.org/pdf/2609.20575v1"
tags: ["query:diff-physics"]
score: 8.0
evidence: 结合可微仿真一阶策略梯度与采样式MPC
tldr: 视觉运动与操作策略需协调接触，训练计算和显存开销大；一阶策略梯度虽用可微仿真降本，却易收敛到非预期接触。本文提出采样引导策略搜索SGPS，将采样模型预测控制的动作目标精炼与一阶策略优化交替，行为克隆初始化，并在扰动状态和随机动力学下做短时域更新。视觉训练采用解耦一阶策略梯度，把渲染移出计算图，直接由深度观测学习。单GPU在Unitree Go2/G1仿真学会运动、越障、推箱和双臂搬运，精炼优于仅初始化或跟踪；蒸馏策略零样本迁移真实Go2，用机载深度自主小跑、爬行、跨栏并切换。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.20575v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 922, \"height\": 844, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.20575v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1856, \"height\": 510, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.20575v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 885, \"height\": 886, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.20575v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 885, \"height\": 318, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.20575v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 857, \"height\": 408, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.20575v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 922, \"height\": 427, \"label\": \"Figure\"}]"
motivation: 视觉策略需协调接触，训练计算和GPU显存开销高；一阶策略梯度易陷入非预期接触模式。
method: 提出SGPS，将采样MPC动作目标精炼与短时域一阶策略优化交替，并用行为克隆初始化；视觉训练将渲染移出计算图，直接学深度。
result: 单GPU在Unitree Go2/G1仿真学会运动、越障、推箱、双臂搬运；精炼优于仅初始化与跟踪，蒸馏策略零样本迁移真机。
conclusion: SGPS以采样精炼弥补局部梯度缺陷，降低视觉策略训练成本，并实现真实机器人自主多行为切换。
---

## 摘要
学习用于运动与操作的视觉策略需要协调与环境之间的接触，并可能带来可观的计算和GPU内存开销。一阶策略梯度（FoPG）通过可微仿真降低训练成本，但局部优化可能收敛到非预期的接触模式。为解决这一不足，我们提出采样引导策略搜索（SGPS），它将基于采样的模型预测控制所进行的周期性动作-目标细化与一阶策略优化相结合。行为克隆从采样动作初始化策略；随后训练在扰动初始状态和随机化动力学下，交替进行基于采样的细化与短时域FoPG更新。对于视觉策略训练，我们使用一种解耦的FoPG表述，将渲染从计算图中排除，从而无需状态策略教师即可直接从深度观测中学习。在单块GPU上，SGPS在仿真Unitree Go2和G1机器人上学习运动、越障、推箱子和双臂搬运的策略。我们的实验进一步表明，细化对策略学习的改进超出了仅初始化和跟踪所能带来的效果。在硬件部署中，蒸馏后的策略零样本迁移到真实Go2，并利用机载深度信息自主进行小跑、爬行、跨越栏架，并在这些行为之间切换。

## Abstract
Learning visual policies for locomotion and manipulation requires coordinating contact with the environment and can incur substantial computation and GPU memory costs. First-order policy gradients (FoPG) reduce training cost through differentiable simulation, but local optimization can converge to unintended contact patterns. To address this shortfall, we propose Sampling-Guided Policy Search (SGPS), which couples recurring action-target refinement by sampling-based model-predictive control with first-order policy optimization. Behavior cloning initializes the policy from sampled actions; training then alternates sampling-based refinement with short-horizon FoPG updates under perturbed initial states and randomized dynamics. For visual policy training, we use a decoupled FoPG formulation that excludes rendering from the computation graph, enabling direct learning from depth observations without a state-policy teacher. On a single GPU, SGPS learns policies for locomotion, obstacle traversal, crate pushing, and bimanual carrying on simulated Unitree Go2 and G1 robots. Our experiments further show that refinement improves policy learning beyond initialization and tracking alone. For hardware deployment, the distilled policy transfers zero-shot to a real Go2 and uses onboard depth to autonomously trot, crawl, clear hurdles, and switch between these behaviors.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义
- **研究动机**：腿式运动与操作任务中的视觉策略需要协调接触，训练常依赖大量并行仿真、计算与 GPU 显存；视觉观测进一步加重成本。
- **现有方法不足**：
  - 一阶策略梯度（FoPG）借助可微仿真降低训练成本，但局部优化可能收敛到非预期接触模式，例如步态奖励不区分相位时学到“拖步/蹭地”行为。
  - 采样 MPC / DIAL-MPC 能生成动态可行的运动轨迹，但部署时通常需要在线优化、状态估计，难以直接变成容忍偏差的视觉策略。
  - 常见教师-学生视觉训练需要先训练特权状态教师，再蒸馏视觉学生，流程更重。
- **整体含义**：论文提出 **SGPS（Sampling-Guided Policy Search）**，将采样 MPC 的周期性动作目标精炼与解耦 FoPG 结合，用搜索提供监督信号，同时保持跟踪参考固定，从而在单 GPU 上直接训练视觉策略，并实现真实 Go2 的零样本部署与自主行为切换。

## 2. 方法论
- **核心思想**：采样 MPC 先生成可行参考并初始化策略；训练中反复用采样搜索生成新的动作目标，与短时域 FoPG 跟踪更新交替进行。跟踪参考轨迹保持固定，精炼只更新动作目标，避免行为目标漂移。
- **参考生成**：
  - 采用类似 DIAL-MPC 的退火采样：用样条节点表示动作序列，迭代中对节点加噪、裁剪、解码候选轨迹。
  - 用指数化 rollout 回报权重做 MPPI 式加权更新，执行首动作并滚动重规划。
  - 采样目标函数无需可微；参考轨迹需通过预定义任务级成功测试后固定为跟踪参考。
- **行为克隆初始化**：
  - 对干净参考状态加入噪声副本，配对参考动作；动作转为 pre-squash 目标，使用 `atanh(clip(...))`。
  - BC 损失为策略均值输出与目标之间的均方误差，平均到样本和动作维度。
- **SGPS 策略优化**：
  - 训练分为 `J` 个 block，每个 block 先做一次采样精炼，再做 `B` 个 actor-critic epoch。
  - 跟踪观测包含本体感知、参考关节位姿/速度、基座误差、上一动作、相位时钟等；Go2 为 53 维，G1 推箱为 106 维，G1 搬运为 118 维。
  - 跟踪奖励由存活项和多项指数误差项组成，覆盖基座姿态、速度、足高、关节角等。
  - 精炼时从当前训练状态出发，用当前策略 rollout 或零动作作为名义计划，再采样搜索；返回片段需满足最小分数和相对种子计划的边际提升才被接受。
  - 接受片段中的“观测-动作”对被转为固定目标，加入目标缓冲区。
  - 总损失为跟踪损失加克隆损失：`L_SGPS = L_track + λ_j L_BC`，其中 `λ_j` 随 block 线性衰减，减少固定目标后期影响。
- **视觉策略学习**：
  - 使用解耦 FoPG：actor 在 `sg[o]` 上求值，渲染不进入计算图，但物理仍可微，因此梯度可通过动作和动力学传回视觉编码器与 actor。
  - 视觉专家直接使用三帧 `64×64` 自视角深度，无需特权状态教师。
  - 为缓解“图像相同但本体噪声不同”的捷径，初始化批次中随机将部分深度栈置零。
  - critic 训练时可用特权状态，部署时丢弃。
- **理论联系**：
  - 命题 1 表明，在生成参数处，固定搜索目标的克隆梯度方向与确定性解耦 FoPG 方向等价；但后续有限步更新不一定等价。
- **蒸馏与部署**：
  - 用 DAgger 将 Go2
